spring:
  application:
    name: api-backend
  profiles:
    active: @profiles.active@
feign:
  client:
    config:
      default:
        connectTimeout: 60000
        readTimeout: 600000
rocketmq:
  name-server: www.lingjiatong.cn:30160
  producer:
    group: api-backend
    send-message-timeout: 3000
  consumer:
    group: api-backend
