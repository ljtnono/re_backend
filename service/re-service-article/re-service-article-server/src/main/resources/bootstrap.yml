spring:
  application:
    name: re-service-article-server
  profiles:
    active: @profiles.active@
feign:
  client:
    config:
      default:
        connectTimeout: 60000
        readTimeout: 600000
